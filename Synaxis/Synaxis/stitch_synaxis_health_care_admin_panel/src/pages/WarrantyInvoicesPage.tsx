import { useQuery } from '@tanstack/react-query';import { Link } from 'react-router-dom';import { FileCheck2,Plus } from 'lucide-react';import { supabase } from '../lib/supabaseClient';import { money,shortDate } from '../lib/format';export function WarrantyInvoicesPage(){const{data=[],error,isLoading}=useQuery({queryKey:['warranty-invoices'],queryFn:async()=>{const{data,error}=await supabase.from('warranty_invoices').select('*,warranty_invoice_items(id)').order('created_at',{ascending:false});if(error)throw error;return data??[]}});return <><header className="mb-6 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-label-md uppercase text-primary">Sales warranty</p><h2 className="font-heading text-headline-lg">Warranty Invoices</h2></div><Link to="/warranty/new" className="primary-button flex items-center justify-center gap-2 px-5 py-3"><Plus size={18}/>New invoice</Link></header><div className="glass-card rounded-xl p-5">{error&&<p className="text-error">{error.message}</p>}<div className="overflow-x-auto"><table className="data-table"><thead><tr><th>Invoice</th><th>M/s</th><th>Date</th><th>Items / Summary</th><th>Net Amount</th><th/></tr></thead><tbody>{isLoading?<tr><td colSpan={6}>Loading...</td></tr>:data.length?data.map((x:any)=><tr key={x.id}><td className="font-mono text-primary">{x.invoice_number}</td><td>{x.customer_name_snapshot}</td><td>{shortDate(x.invoice_date)}</td><td>{x.warranty_invoice_items?.length??0} / {x.summary_no||'-'}</td><td>{money(Number(x.net_amount))}</td><td><Link to={`/warranty/${x.id}`} className="secondary-button">View / Edit</Link></td></tr>):<tr><td colSpan={6} className="py-12 text-center"><FileCheck2 className="mx-auto"/>No warranty invoices.</td></tr>}</tbody></table></div></div></>}

